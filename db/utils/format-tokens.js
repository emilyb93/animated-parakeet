const getTokens = require("../tokens.js");

getTokens()

async function formatTokens(tokens){
    let section = {}
    const sections = []

    //section - heading and content and main heading
    // {type: "heading", tokens: [{headings:paragraph}]}
}